package com.example.spendbase.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.example.spendbase.R


val AppTypography = Typography()

val HeadlineMedium28 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_bold)),
    fontSize = 28.sp
)

val HeadlineMedium34 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_bold)),
    fontSize = 34.sp
)

val HeadlineSmall17 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_bold)),
    fontSize = 17.sp
)

val TitleMedium16 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_semibold)),
    fontSize = 16.sp
)

val LabelLarge15 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_semibold)),
    fontSize = 15.sp
)

val LabelMedium13 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_semibold)),
    fontSize = 13.sp
)

val LabelSmall12 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_medium)),
    fontSize = 12.sp
)

val BodyLarge17 = TextStyle(
    fontSize = 17.sp,
    fontFamily = FontFamily(Font(R.font.sfpro_medium))
)

val SemiBold10 = TextStyle(
    fontSize = 10.sp,
    fontWeight = FontWeight.SemiBold
)

val Regular10 = TextStyle(
    fontFamily = FontFamily(Font(R.font.sfpro_medium)),
    fontSize = 10.sp
)